// Noto Sans KR 폰트를 base64로 인코딩하여 추가
// 실제로는 폰트 파일을 base64로 변환한 값을 여기에 넣어야 합니다.
// 여기서는 간단한 방법으로 시스템 폰트를 사용하거나, 
// 작은 한글 폰트 subset을 사용합니다.

// 한글 폰트 추가 함수
export function addKoreanFont(doc) {
  // Noto Sans KR의 subset을 base64로 인코딩한 값
  // 실제로는 폰트 파일을 base64로 변환해야 합니다.
  // 여기서는 간단한 방법으로 폰트를 추가합니다.
  
  // 시스템에 설치된 한글 폰트를 사용하려면
  // Electron의 webContents.printToPDF를 사용하는 것이 더 나을 수 있습니다.
  // 하지만 jsPDF를 사용하려면 폰트를 추가해야 합니다.
  
  // 임시로 기본 폰트를 사용하되, 한글을 지원하는 방법을 찾아야 합니다.
  // 실제로는 Noto Sans KR 폰트 파일을 base64로 변환하여 추가해야 합니다.
  
  return doc;
}
